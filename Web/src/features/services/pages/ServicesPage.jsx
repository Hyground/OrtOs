import { useDocumentTitle } from '@/hooks/useDocumentTitle'

export function ServicesPage() {
  useDocumentTitle('Servicios y especialidades')

  return (
    <section>
      <h1>Servicios y especialidades</h1>
      <p>Pendiente de diseño.</p>
    </section>
  )
}
